# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# fetch-docs.mjs' own local cache -- always rebuilt from a real,
# checksummed release, never committed
/.docs-cache

# build-search-index.mjs' own output -- regenerated every build from
# .docs-cache, never committed
/public/search-index.json

# build-sidebar-index.mjs' own output -- same real convention
/public/sidebar/

# typescript
*.tsbuildinfo
next-env.d.ts

# AGENTS.md and CLAUDE.md were ignored here with the comment "Generated
# by next build for AI agents, not source". That is not true: removing
# both and running a full `next build` regenerates neither. The rule came
# across with the scaffold and was never checked, and it is the whole
# reason this repo had no committed CLAUDE.md while ubx-docs-providers,
# which carries no such rule, has both files committed.
