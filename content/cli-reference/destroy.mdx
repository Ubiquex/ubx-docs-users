---
title: "ubx destroy"
description: "There is no ubx destroy. Use ubx terminate."
---

`ubx destroy` isn't a real command. It exists purely to catch Terraform
muscle memory and redirect it to the real one.

## Usage

```bash
ubx destroy <address>...
```

## Example

```bash
ubx destroy payments.aws_sqs_queue.pipeline-events
```

```bash
destroy: there is no "ubx destroy" -- use "ubx terminate" (same
destroys[] machinery underneath, ledger-derived, no authoring document
needed)
```

## See also

- [`ubx terminate`](/cli-reference/terminate), the real command
- [Teaching Errors](/concepts/teaching-errors)
